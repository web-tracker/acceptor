export default class Environment {
  OS: string;
  browser: string;
  browerVersion: string;
  device: string;
  deviceVersion: string;
  IPAddress: string;
  // Network Provider Company
  network: string;
}